Flask>=3.0
flask-cors>=4.0
flask-limiter>=3.5
opensearch-py>=2,<3
pandas>=2.2
pyarrow>=15
# Not a direct import of ours -- pandas pulls it in -- but the floor is
# load-bearing and pandas does NOT enforce it (pandas 2.x still accepts numpy
# 1.26). The MSR-file pickles in MinIO are written by an upstream process on
# numpy 2, and a numpy-2 pickle names `numpy._core.*` as its reconstructor -- a
# module that does not exist in numpy 1, so get_pickle() dies with
# `ModuleNotFoundError: No module named 'numpy._core'` (hit on the cloud
# 2026-08-03). The reverse direction is safe: numpy 2 keeps a `numpy.core`
# shim, so numpy-1 pickles still load. Raising the reader's floor is the fix.
numpy>=2
redis>=5
minio>=7
httpx>=0.27
# ftp_handler/proxy/proxy_downloader.py imports this at module level, and that
# file ships in the deploy bundle. It only resolved until now because
# opensearch-py happens to pull it in transitively -- declared here so the
# cloud install cannot lose it to a dependency change upstream.
requests>=2.31
python-dotenv>=1.0
apscheduler>=3.10
langchain>=1,<2
langgraph>=1,<2
langchain-openai>=1,<2
