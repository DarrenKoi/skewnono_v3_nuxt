# msr_image feature package. The blueprint (`bp`) is re-exported here once
# routes.py exists (added in the routes task); kept import-free until then so
# submodule imports (contracts/errors/paths) don't require routes.py yet.
