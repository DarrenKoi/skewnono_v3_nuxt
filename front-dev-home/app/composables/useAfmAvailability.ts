/**
 * AFM Metrology is still under construction and stays hidden from everyone
 * until it is ready to show.
 *
 * Flip this to `true` to bring the whole feature back: the landing-page card
 * reappears and `/afm/*` stops redirecting. Nothing else needs editing.
 */
export const AFM_ENABLED = false

export const useAfmEnabled = () => AFM_ENABLED
