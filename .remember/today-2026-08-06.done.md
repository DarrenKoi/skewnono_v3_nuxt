## 07:50 | main
Fixed `/api/cdsem/live-alarm` feature mapping (feature_map.py: +_TOOL_PAGE_RULES); corrected comment (policy.py); coverage (test_feature_map.py); verified

## 07:28 | main
Designed manual search for chat (OpenSearch hybrid: Nori+BGE-M3+reranker); ingestion contract (Evidence schema, AccessScope filters, deterministic IDs); tool exposure (available_sources())