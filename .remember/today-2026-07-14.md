## 08:23 | main
Nav bug (info pages can't→main); extended FeatureTabs render + fallback toolType; default layout; round-trip verified; f5a5dbe

## 16:58-17:09 | main
MDC redesign (시계열/비교 tabs, boxplot, trajectory/trends comparison); BM/PM overlay (toggle, line markers) ×5 tabs; SCE smoothed (sinusoids vs noise), line/radar ScePanel.vue, coeff smoothed; activity: sem-model route `/api/activity/sem-models` + composable, per-model SEM card; Recipe 현황 (TAT/Align/Meas tabs, pending build); tightYRange guard; 10-task subagent approved; E2E verify
## 08:51-09:16 | main
RecipeStatusView.vue (Recipe TAT/Align Fail/Meas Fail tabs) wrapped SkNavPill + dashboard-surface (contrast per DESIGN.md); styled card + segmented track; 'RECIPE 현황'/'목록' pod matching meta-bar

## 09:08-09:25 | main
Back-nav fix: FeatureTabs on info paths; intro/endpoints/activity/settings hub → default (consistency); '스큐 관리' tab hidden (FeatureTabs.vue:32); route/view retained; pushed
## 09:51 | main
Removed dead Quick Access comp (landing pages/index.vue); comprehensive triage redesign discussed, deferred.

## 09:56 | main
AFM search: date+time display, +/− toggle group removal (useAfmDetailApi.ts, SearchBar.vue, pages/afm/[tool]/index.vue); verified live.

## 10:00 | main
Started skewvoir 측정 결과 search: opensearch (tool/recipe/lot/date/msr filters), localStorage latest measurement; codebase exploration begun.

## 10:25 | main
Cloudflare tunnel (app preview) blocked [::1]:3000 loopback from tablet Tailscale; switched npm dev:remote w/ 0.0.0.0 bind, updated allowedHosts (.ts.net MagicDNS), killed public tunnel; added tmux guidance (SSH persistence).
## 10:28 | main
Designed skewvoir search (측정 결과 검색) w/ OpenSearch backend, auto-detect parser, facets (FAB/EQ/recipe/date), localStorage recent-views, 50-row paging, 60d retention.

## 10:51 | main
Implemented X-ID access blocking w/ admin-managed exceptions: `access_control/` module (JSON exceptions, denied-attempts buffer, `/api/admin/access` endpoints), `_auth/admin.py` shared, middleware 403 gate, backend tests.

## 12:52 | main
Fixed recipe-status refactoring regressions: made activeTab reactive-computed from route.query.tab; added KeepAlive for tab switches; preserved ?tab= on same-feature nav; removed dead legacy-route slugs, unreachable dual-column mode FailIssueView.
## 12:54-13:12 | main
X-ID access-control: spec (2026-07-14-access-control-design.md); impl access_control/ + admin.py (routes, exceptions, JSON), DeniedScreen, /admin/access, /api/admin/logs fix; 14+ tests✓; live verified; 8-agent review (30+ findings, 5 bugs fixed); commit-ready

## 12:56-14:30 | main
Skewvoir search: spec (2026-07-14-skewvoir-search-design.md) + 9-task SDD plan; OpenSearch 60d, auto-parse (eq/lot/recipe/date/msr), facets, client parser, localStorage ×15; Tasks 1–8 (measHistQuery aedb61e✓166, BE search/facets 2defb69, API 44add14, state, components, 15-recent w/expiry); aedb61e–e5f9efa 6 commits; fixed component-name & recipe-name bugs; all reviews✓; E2E✓

## 13:38-13:43 | main
~/.tmux.conf: C-a, mouse, splits, 100k history; validated live

## 14:18-15:30 | main
Network: killed CF; fixed Nuxt loopback-only bind → .ts.net allowedHosts + --host 0.0.0.0; tablet 100.103.116.55:3000 over Tailscale verified (FE+API)