## 16:33 | main
Fixed idp-tuple-mapping nested-dict handling combined_idp_info (502); regression tests; 5be8101 → main; tracing missing params recipe-open (addressing/focus/pattern/beam-cond/amp/seq).

## 16:35-16:44 | main
Implemented image cache warming (imageWarm + useMsrImageWarmer composable → Dashboard/SemImage, prevents 502 delayed loads); favicon data: URI nuxt.config.ts (stops re-requests router.replace()); fixed useMsrImageWarmer.ts watch race (added `names` deps—omission blocked warm if context resolved before rows).

## 04:10 | main
Cleaned port 5050: killed Flask dev server (Werkzeug reloader PIDs 53879, 53939); port 3000 clear.