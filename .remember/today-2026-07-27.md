## 21:55-22:15 | main
ruff: 19 findings fixed (10 files); pyproject.toml config + CI gate (vendored excluded); py311 target-version, rules pinned (prevents 0.16 breakage); py3.11 compat verified (1321 tests ✓); afm_data_platform→docs/afm/ (2 docs, refs updated).

## 07:49 | main
skewvoir/analysis: responsive design bug (misaligned components <narrow; ✓ 1600px).
## 07:55 | main
Fixed Dashboard.vue wafer-map grid overflow via resp min-h-0 (700–1280px tested, no regressions); sweeping 4 other views for same pattern.

## 08:18-08:35 | main
Diagnosed skewvoir FDC/param misalignment: sequence.ts:171-175 unions all sequences vs should filter to cd_value/selected param; found correct join logic in relationships.ts:178-185; identified mock data cartesian product masks; clarified each param has own numbered sequences.

## 08:20-08:51 | main
Removed handoff feature (4 buttons, HandoffButton.vue, handoffs.ts from StatBar.vue & ParamNav.vue & useSkewvoirAnalysis.ts; 784✓); added param defaulting (skip "" dummy, pick next available); pre-armed gallery priority filter; added empty-param handling via URL sentinel (routeQuery.ts) + label helper (paramOrder.ts) + mock dummy MP.

## 08:41 | main
Added 8 new tests for param/gallery helpers (792✓); E2E verified pre-armed filter (4/66 shown), empty-param defaulting, correlation axis pickers.

## 08:43 | main
Split skewvoir FDC view (idx 3, single-scope) from time-series (idx 4); param-scoped axis (sequence.ts axisMode); mock per-param interleave; len(rows)==len(dynamic_fdc) invariant; design spec written.

## 08:47 | main
Removed msr_file.txt, repointed 6 refs; added README.md + 5 schema txt (sem_list, recipe_name_list, idp_ver, msr_image_ftp, live_alarm_board); aligned docs to office adapter impl.