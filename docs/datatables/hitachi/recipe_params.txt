device-statistics 의 recipe-params mock data 입니다.

device(lot_cd) 1개는 recipe 100~200개를 가지며, recipe 1개는 파라미터 다수를
측정합니다. 파라미터마다 측정 point 수가 다릅니다. 프론트엔드 ruleEngine.ts 의
RecipeInput 형태와 1:1 로 맞춰 raw 로 내려보냅니다. 위반(R3 cap)·outlier 판정은
프론트엔드가 client-side 로 수행합니다 (백엔드는 raw 만 제공).

GET /api/cdsem/device-statistics/recipe-params?lot_cds=R000,R001

응답: RecipeParamsRow 배열 (recipe 당 1행)

컬럼 -> 타입: 설명

lot_cd -> string: device 식별자. r3-device-grp / device-desc 의 lot_cd 와 조인됩니다.
recipe_id -> string: recipe 식별자. idp registry 의 full_name 이며 형식은
                     "class_name/recipe_name" 입니다 — "/" 앞이 class_name
                     (recipe_name_list.txt L56, user-confirmed 2026-07-29).
                     **이름의 사전순은 oper_seq 와 무관합니다** — 이름은 MMDM 이
                     부여하고 공정 순서는 planstep 이 정하는 별개의 축입니다.
                     mock 도 두 축을 독립으로 만듭니다(class 를 lot·idx digest 로
                     고름). 예전 mock 의 RCP-<lot_cd>-NNN 은 두 축이 완전상관이라
                     lot 상세의 정렬 토글이 집에서 아무 변화도 못 보여 줬습니다.
                     mock 의 class/recipe 어휘 자체는 지어낸 것입니다
                     (OFFICE-VERIFY — 실제 class 목록 미확인).
fac_id -> string: fab 코드. R3 또는 M11~M16 입니다.
ctn_desc -> string: recipe 설명 문자열입니다.
prod_catg_cd -> string: 제품 카테고리. DRAM, NAND, FLASH, Tech, Advanced 중 하나입니다.
recipe_class -> string: Main 또는 Sample 입니다 (D2).
family -> string: Core, Pool, VG_RTC_Cubic 중 하나입니다 (D3).
phase -> string|null: t-EV, EV, TV, PV 또는 null 입니다 (D3).
memory_class_auto -> string: DRAM, NAND, unknown 중 하나입니다. Tech/Advanced 는 unknown(수동) 입니다 (D7).
parameters -> records: 측정 파라미터 배열입니다. 각 원소는 {name, point_count, mother} 입니다.
                       **순서가 의미를 갖습니다** — recipe 가 파라미터를 적어 둔
                       순서 그대로이며, WAFER 계열이 맨 앞에 오는 것이 관례입니다
                       (user-confirmed 2026-08-05). 어댑터는 이 순서를 바꾸지
                       않습니다. 이름순으로 정렬하면 "WAFER" 가 알파벳상 거의
                       끝이라 가장 중요한 파라미터가 목록 맨 아래로 밀립니다 —
                       office 어댑터가 실제로 그랬고 2026-08-05 에 걷어냈습니다.

parameters 원소
name -> string: 파라미터 이름. 타입은 longest-prefix(EDGE_EX > EDGE > WAFER > LEVEL)로 파생되며, 그 외는 OTHER 입니다.
point_count -> int: 해당 파라미터의 측정 point 수입니다.
mother -> bool: 이 파라미터 자신이 mother 인지입니다 (idp 의 Mother_Para, office 확인 2026-07-28).
region -> int|null: 이 파라미터가 속한 image definition 묶음입니다 (idp 의
                    idp_image_info.Region, user-confirmed 2026-08-18). 같은 region
                    이 한 SEQ 그룹입니다 — 아래 "SEQ 그룹" 절을 보십시오.
                    null 은 "이 문서에서 Region 을 읽지 못했다" 입니다.

측정이 아닌 파라미터 — Dummy / Align (user-confirmed 2026-08-05)

둘 다 실물에 있는 파라미터 이름이며, **CD 를 재는 파라미터가 아닙니다**.

  Dummy  자리를 채우는 placeholder. 재는 대상 자체가 아닙니다.
  Align  정렬(addressing)용. 측정이 아니라 측정을 위한 준비라, 그 point 수는
         "얼마나 많이 쟀는가" 라는 질문의 답에 들어가면 안 됩니다. 설비 알람도
         align(9006)과 meas(9007)를 다른 사건으로 셉니다.

이름 표기 (user-confirmed 2026-08-05)

파라미터 이름은 대체로 **전부 대문자**입니다 (WAFER_CD, EDGE_L, OVL_X …).
그런데 이 둘만 그렇지 않습니다 — "Dummy", "Align" 처럼 첫 글자만 대문자입니다.

그래서 두 걸러내는 자리 모두 **대소문자를 무시**합니다. 코드나 룰 데이터에
대문자로 적힌 곳들(NON_MEASUREMENT_PARAMS, name_override 의 patterns)은 비교
전에 이름을 대문자로 올리기 때문에 쓰는 **정규형**이지, 데이터의 모습이
아닙니다. 표기를 하나로 못박는 회귀(`name == "DUMMY"` 같은)는 mock 이 실물
표기를 쓰는 한 집에서 바로 걸립니다 — 그래서 mock 도 "Dummy"/"Align" 로
만듭니다.

빠지는 자리가 서로 다르니 함께 보아야 합니다.

  outlier(outlierDetect.isOutlierExemptParam)
      Dummy·Align **둘 다** 중앙값 기준선에서도 초과 목록에서도 빠집니다.
      남겨 두면 Align 의 큰 point 수가 기준선을 끌어올려 정상 파라미터의 진짜
      과다 측정이 문턱 아래로 숨습니다.

  판정(계측 룰)
      Dummy **만** 면제입니다 — Sample 셀의 name_override 에 cap=None 으로
      들어 있습니다 (providers/rules.py). Sample 셀은 _other cap 이 0 이라
      면제가 없으면 Dummy 가 point 1 만 있어도 항상 위반이고, 그 위반은 recipe
      를 고쳐 없앨 수 있는 종류가 아니라 고칠 수 있는 진짜 위반을 밀어냅니다.
      Align 은 아직 판정 면제가 아닙니다 — 요청 범위가 outlier 집계였습니다
      (OFFICE-VERIFY: Sample recipe 의 Align 이 위반으로 잡히는지 미확인).

두 자리를 한 스위치로 묶지 않은 것은 성격이 다르기 때문입니다 — outlier 는
서술적 통계, cap 은 규범입니다. 묶으면 룰 한 칸을 고칠 때 중앙값 기준선까지
따라 움직입니다.

이름 매칭은 룰 데이터의 affix 와 같은 의미입니다: 그 낱말로 시작하거나 끝나면
해당, 이름 한복판에 우연히 든 것은 제외.

SEQ 그룹 — region 컬럼 (user-confirmed 2026-08-18)

idp 의 한 Region 은 **image definition 1개**이고, 같은 Region 인 파라미터들이 한
SEQ 그룹입니다. 화면에 "1/8, 2/8, 3/8 …" 으로 보이는 그 묶음이며 분모는 Last_SEQ
입니다. 그룹 안에서 Mother_Para 인 하나가 그 image 의 주인(mother)이고, 나머지는
son 입니다.

    Parameter   Mother_Para   Region   화면
    WAFER       True          1        1/8
    CELL_SP     False         1        2/8
    LWR         False         1        3/8
    …

son 은 mother 와 **같은 image** 에서 자기 cd_value 를 꺼내므로, 그룹 전체가 한
번의 측정입니다 — son 을 재는 데 드는 point 는 따로 없습니다.

★ 그래서 계측 룰 판정이 son 을 **mother 의 cap** 으로 잽니다.

  WAFER 13 / LEVEL 4 는 모든 룰 셀에 공통이고 바뀐 적이 없습니다
  (providers/rules.py 의 _MAIN_COMMON). 그런데 2026-08-18 이전의 판정은 파라미터
  이름만 보고 cap 을 골랐기 때문에, WAFER(13) mother 의 son 인 CELL_SP·LWR 이
  이름이 OTHER 라는 이유로 _other(9)에 걸려 **위반**으로 잡혔습니다. 룰이 틀린
  것이 아니라 "무엇을 무엇으로 재는가" 가 틀렸고, 그 위반은 recipe 를 고쳐서
  없앨 수 있는 종류가 아니라 고칠 수 있는 진짜 위반을 밀어냅니다.

  물려받는 것이지 면제가 아닙니다 — LEVEL(4) mother 의 son 이 13 이면 여전히
  위반입니다. 그룹이 통째로 판정 밖으로 나가면 그것대로 신호를 잃습니다.
  구현은 프론트엔드 utils/ruleEngine.ts 의 groupCaps / effectiveCap 한 곳입니다.

  region 이 null 인 파라미터는 묶을 근거가 없으므로 예전처럼 자기 cap 으로
  판정합니다. 순서로 추측하지 않습니다 — 추측이 틀리면 son 에게 엉뚱한 mother 의
  cap 이 가고, 그 차이는 예외가 아니라 위반 수로만 나타납니다.

★ son 의 point 수는 mother 를 따라가지 않고, 다만 넘지 않습니다.

  실물 문서 1건이 {'EDGE': 10, 'LEVEL': 4, 'WAFER': 10} 이고 그 WAFER 가 mother,
  LEVEL 이 son 입니다 (idp_ver.txt) — son 이 mother 보다 **적게** 잰 경우입니다.
  반대로 묶음이 통째로 같은 값인 경우(WAFER 13 에 son 도 13)도 있습니다
  (user-confirmed 2026-08-18). 둘 중 하나가 규칙인 것이 아니라 둘 다 나타납니다.

  OFFICE-VERIFY: "son ≤ mother" 는 확인된 문서 1건과 "같은 image 를 쓴다" 는
  사실에서 따라오는 **추론**입니다. son 이 한 site 에서 여러 값을 재어 mother 보다
  point 가 많아지는 형태라면 틀립니다. 판정은 어느 쪽이든 옳게 동작하므로
  (넘으면 위반), 틀렸을 때 잃는 것은 mock 의 사실성뿐입니다.

  OFFICE-VERIFY: 관찰된 묶음 하나는 8개짜리인데, mock 의 묶음은 MOTHER_SHARE
  (0.25~0.45)가 정하므로 2~4 개입니다. 그 비중은 recipe_population 과 공유하는
  값이라 한쪽만 넓히면 두 표면의 mother 수가 갈라집니다.

mother 컬럼 — 무엇에 쓰이는가 (user-confirmed 2026-08-04)

son 파라미터는 mother 와 **같은 image** 에서 자기 cd_value 를 얻으므로 측정
시간(TAT)을 움직이는 것은 mother 수입니다. 그래서 comparison 화면의
mother_normal 버킷은 이 플래그로 파라미터를 좁혀서

  - para_* 구간 버킷 (Lot 요약의 "para 분포"·"para 합계") —
    point 수를 다섯 구간(>16 / 14~16 / 10~13 / 6~9 / <=5)으로 나눈 개수입니다
    (2026-08-10 합의, docs/datatables/hitachi/idp_ver.txt)
  - 계측 룰 검증 (health / violations / 판정 범위)
  - 측정 프로파일 (중앙값 / outlier)

셋을 **모두** 같은 mother 집합 위에서 계산합니다. 프론트엔드에서는
utils/lotHealth.ts 의 scopeRecipesToBucket 한 곳이 이 좁히기를 담당하며, 세
소비처가 같은 배열을 받습니다 — 각자 좁히게 두면 언젠가 한쪽만 고쳐져 표 한
행의 열들이 서로 다른 모수집단을 말하게 됩니다.

mother 를 가진 파라미터가 하나도 없는 recipe 는 mother_normal 버킷에 아예
들어가지 않습니다 (recipe 와 파라미터 **양쪽**을 좁힙니다).

★ 출처 미해결 (2026-08-04) — 이 컬럼은 **사무실에서 아직 채울 수 없습니다.**

  Mother_Para 가 확인된 유일한 곳은 장비 FTP 의 {idp_name}.idp 원본 파일이고
  (docs/datatables/hitachi/recipe_idp.txt), 이 응답의 parameters 원천인
  cdsem_idp_ver.parameters 는 {name: point_count} 라 플래그를 담을 자리가
  없습니다. recipe 1건당 파일 1개를 내려받아야 하므로 device 4000개 규모에서는
  그 경로를 쓸 수 없습니다.

  어댑터는 blob 이 [{name, point_count, ...}] 형태로 바뀌어 플래그를 실어 주면
  그대로 읽고(키 후보: mother / Mother_Para / mother_para), 아니면 경고 로그를
  남기고 mother 를 전부 false 로 둡니다. 그 경우 mother_normal 버킷은 비어
  있게 되며, 이는 규칙을 데이터에 그대로 적용한 결과입니다.
  해결 후보는 device_statistics/MIGRATION.md 의 "mother_para 출처" 절입니다.

recipe 이름 토큰 — 특수 측정 job
(_WCDU/_FCDU/_FULL user-confirmed 2026-08-04,
 _HALF/_BCDU/_MTX 및 "_*CDU" 패턴 user-confirmed 2026-08-05,
 토큰 뒤에 꼬리가 붙는 "_BCDU_NEW" user-confirmed 2026-08-11)

실물 recipe 이름은 "_S"/"SE"(Sample) 외에 아래 토큰을 **포함**할 수 있습니다.

  "_*CDU"   CDU(CD uniformity) 측정 job. **목록이 아니라 패턴입니다** — 앞
            글자는 어떤 map 을 재는지를 뜻할 뿐이고(W=wafer, F=field, B=…)
            종류가 늘 수 있습니다. 확인된 예: _WCDU, _FCDU, _BCDU.
  "_FULL"   full-map 측정 job.
  "_HALF"   half-map 측정 job.
  "_MTX"    matrix 측정 job.

CDU 를 패턴으로 적어 두는 것이 중요합니다. 처음에는 _WCDU/_FCDU 만 열거했고,
그래서 실물의 _BCDU 가 어느 제외 경로에도 걸리지 않고 분석에 그대로 섞여
들었습니다 (2026-08-05). 접미사를 하나씩 세는 방식 자체가 이 데이터에 맞지
않습니다.

**토큰의 위치도 고정되어 있지 않습니다.** 실물에 "_BCDU_NEW" 처럼 토큰 뒤에
꼬리가 더 붙는 이름이 있습니다 (user-confirmed 2026-08-11). 이름의 끝을 보는
규칙은 이 이름을 정상 recipe 로 흘려보냈고, 이는 _BCDU 때와 같은 종류의
누락입니다 — 이 job 을 가리키는 것은 토큰이 놓인 자리가 아니라 토큰 자체입니다.
그래서 판정은 "이름이 그 토큰으로 끝나는가" 가 아니라 "이름에 그 토큰이 들어
있는가" 입니다. 앞의 밑줄만은 남깁니다 — 그 경계까지 놓으면 이름 안에서 우연히
만들어진 글자 조합(예: "…001CDU")에 정상 recipe 가 조용히 제외됩니다.

이 토큰은 이름의 일부라서 cdsem_idp_ver.full_name 조인 키를 깨지 않습니다.
recipe_class 파생 규칙(_S/SE)과도 겹치지 않으므로 서로 간섭하지 않습니다.

이 job 들은 웨이퍼를 통째로 훑는 것이 목적이라 **측정 규모가 정상 recipe 와
다른 차원**입니다. 그래서 프론트엔드의 두 분석 모두에서 빠집니다.

  판정(lotHealth.isExemptJob) — 계측 룰 판정 범위에서 **분자·분모 모두**
      제외합니다. gray(분모에 남는 판정 보류)와 다릅니다.
  outlier(outlierDetect)     — point_count 중앙값 **기준선에서도**, 초과 표시
      대상에서도 제외합니다. 남겨 두면 이 job 들의 큰 point_count 가 기준선을
      끌어올려 정상 recipe 의 진짜 과다 측정이 문턱 아래로 숨습니다.

Mock 생성 규칙
1. lot_cd 별 결정론적 seed(statistics._seed_for)로 recipe·parameter 를 생성합니다.
2. 약 8% recipe 는 기타(OTHER) 파라미터가 많은 bloated recipe 입니다 (D10 신호 검증용).
3. 약 5% recipe 는 EDGE point 수가 또래 대비 큰 outlier 를 포함합니다 (outlier 뷰·R3 cap 검증용).
4. 약 10% recipe 는 특수 측정 job 이름
   (_WCDU/_FCDU/_BCDU/_FULL/_HALF/_MTX/_BCDU_NEW)을 갖고,
   그 recipe 의 point_count 는 정상 recipe 의 8배입니다 (판정·outlier 두 제외 경로
   검증용). 이 목록은 **표본**일 뿐 판정 기준이 아닙니다 — 기준은 위의 "_*CDU"
   패턴이고, _BCDU 를 표본에 넣어 둔 이유는 열거 목록에 없던 이름이 실제로 새어
   나갔기 때문입니다. _BCDU_NEW 는 그 다음 층을 지킵니다 — 나머지 여섯은 토큰으로
   끝나므로 끝에 고정한 옛 규칙으로도 잡혔고, 그래서 "어디에 있든 잡는다" 는
   성질이 집에서 한 번도 관찰되지 않았습니다.
   배율 자체는 OFFICE-VERIFY 입니다 — "자릿수가 다르다" 는 성질만 재현하고
   절대값은 흉내 내지 않습니다. 배율이 1 이면 제외 규칙을 지워도 테스트가 조용히
   통과하므로, 이 값은 장식이 아니라 회귀의 조건입니다.
5. Sample recipe 에는 Dummy(point 1)와 Align(point 3)이 붙습니다.

   ★ 둘 다 실물 point 수가 **1~3** 입니다 (user-confirmed 2026-08-10). 이 자리는
     2026-08-05 에 Align 40, 2026-08-10 에 잠시 16 이었고 둘 다 지어낸 값이었습니다.
     40 이 오래 눈에 띄지 않은 것은 정확 일치 버킷에서 16/13/9/5 어디에도 맞지
     않아 아무 버킷에도 안 들어갔기 때문이고, 버킷이 구간이 되자 para_over_16 으로
     들어가 "recipe 의 30% 가 16 초과 파라미터를 갖는" 그림을 만들었습니다
     (실물 3% — idp_ver.txt).

   ★ 나타나는 recipe 와 위치 (user-confirmed 2026-08-10)

     Dummy·Align 은 recipe 마다 늘 있는 것이 아니라 **일부 recipe 에 가끔**
     나타나고(Sample 전용이 아닙니다), 나타날 때는 parameters_list 의 **맨 앞**에
     옵니다 — 정렬은 측정보다 먼저 하는 준비 작업이라 순서가 곧 그 뜻입니다.

     그래서 걸러 내는 판정이 이름만이 아니라 **이름 + 맨 앞** 입니다. 이름만
     보면 목록 뒤쪽의 "CD_ALIGN" 같은 **진짜 측정 파라미터**까지 지워지고, 그
     손실은 예외가 아니라 para 합계가 조금 작아지는 것으로만 나타납니다.
     맨 앞의 연속된 것을 전부 뺍니다 — Dummy 와 Align 이 함께 오면 둘 다 앞에
     있어서, 문자 그대로 한 개만 빼면 두 번째가 통계에 남습니다.

     구현은 백엔드 para_buckets.measurement_parameters, 프론트엔드
     outlierDetect.dropLeadingHelperParams 두 곳이고 같은 규칙입니다.

     2026-08-05~2026-08-10 의 mock 은 이 둘을 Sample recipe 에만, 그것도 **맨
     뒤**에 붙였습니다. 위치가 뒤집혀 있었으므로 위치 기반 규칙이라면 아무것도
     걸러지지 않았을 자리입니다.

   ★ para_* 집계는 **측정 파라미터만** 셉니다 (user-confirmed 2026-08-10).
     Dummy·Align 은 "얼마나 많이 쟀는가" 의 답에 들어가지 않으므로 버킷 집계에서
     빠집니다. 실물의 "16 초과는 전체 파라미터의 2~5%" 도 이 둘을 빼고 센 값입니다.
     거르는 자리는 para_buckets.is_measurement_param 하나이며, 프론트엔드
     outlierDetect.isOutlierExemptParam 과 같은 규칙(낱말로 시작하거나 끝남)입니다.

   ★ 두 제외 경로가 집에서 눈에 보이게 하는 방식이 서로 다릅니다.

       Dummy 1 — Sample 셀의 _other cap 이 0 이라 1 이면 곧바로 위반입니다.
                 판정 면제가 빠지면 위반 수가 늡니다. (값이 **커서** 일합니다.)
       Align 3 — outlier 문턱은 중앙값×2 입니다. 작은 값이라 제외를 지우면
                 중앙값이 6 -> 5 로 **내려가고** 문턱도 12 -> 10 이 되어, 정상
                 범위의 측정 파라미터가 outlier 로 잡힙니다. (값이 **작아서**
                 일합니다.)

     Align 쪽 회귀의 방향이 2026-08-10 에 뒤집혔습니다. 그전 논리는 "큰 point
     수가 기준선을 끌어올려 진짜 과다측정을 가린다" 였는데, 실물 값이 1~3 이면
     일어나지 않는 일입니다. 규칙이 막는 것은 가려짐이 아니라 **오검출**입니다.
     tests/test_non_measurement_params.py 가 이 관계를 셉니다.

   ★ 16 초과 신호는 이 둘이 아니라 **측정 파라미터 이름**이 나릅니다 — 특수 측정
     job 의 EDGE_*/OVL_*, over_measured 의 EDGE_R. 통계에서 빠지는 이름이 통계를
     만들면 안 됩니다.

   point_count 는 rng 가 아니라
   고정값입니다 — rng 를 한 번이라도 더 굴리면 lot 단위로 공유되는 seed 때문에
   뒤따르는 recipe 의 파라미터가 전부 다른 값이 됩니다.
   Sample 에만 붙이는 것은 확인된 사례가 거기이기 때문입니다(Main recipe 에도
   있는지는 OFFICE-VERIFY). 프론트엔드의 outlier 제외는 recipe 종류를 보지 않으므로
   어디에 있든 똑같이 걸립니다.
6. 파라미터를 **연속 구간**으로 갈라 region(SEQ 그룹)을 붙이고, 각 구간의 머리를
   mother 로 켭니다. 묶음 개수 = mother 개수입니다. 2026-08-18 이전에는 index 를
   무작위로 골라 mother 를 켰는데, 그러면 "mother 가 자기 묶음의 머리" 라는 성질이
   없어 son 을 mother 에 이어 붙일 방법 자체가 없습니다. son 의 point 수는 머리보다
   크면 머리에 맞춰 내립니다(위 OFFICE-VERIFY 참고) — 그러지 않으면 묶음 경계가
   난수라 EDGE(6~14) son 이 LEVEL(1~4) mother 밑에 자주 들어가, **집에서만 존재하는
   위반**이 무더기로 잡힙니다. 비측정 파라미터(Dummy·Align)는 region 이 null 이고,
   mother 가 없는 recipe 도 region 은 붙습니다 — 실물의 Region 은 mother 를 읽을 수
   있는지와 무관하게 존재하고, 프론트엔드가 "머리 없는 묶음" 에서 자기 cap 으로
   되돌아가는 경로가 집에서 실제로 실행되어야 합니다.
7. 약 85% recipe 만 mother 파라를 갖고(보유 시 파라미터의 25~45%), 나머지 15% 는
   mother 가 없어 mother_normal 버킷에서 빠집니다 — "recipe 도 좁힌다" 는 규칙이
   집에서 실제로 실행되게 하려는 것입니다. 이 비율은 OFFICE-VERIFY 입니다(실물
   미확인). mother 보유 여부는 recipe-statistics 의 버킷 멤버십과 **같은 원천**
   (recipe_population 의 RecipeIdentity)에서 나오므로 두 표면이 갈라지지 않습니다.


========================================================================
사무실(Office) 파생 규칙 — 어댑터가 각 컬럼을 무엇에서 만드는가
========================================================================

이 응답에는 **원천 컬럼이 그대로 있는 값이 하나도 없습니다.** recipe 목록은
공정 스텝 index 에서, 파라미터는 IDP index 에서 오고, 분류 컬럼 네 개는
device 의 ctn_desc / recipe 이름에서 파생됩니다. 근거 없이 값을 지어내지 않는
것이 규칙이므로, 판별할 수 없는 값은 빈 문자열이나 null 로 둡니다.

원천 (docs/datatables/hitachi/planstep_r3.txt, ebeam_tas_lot_hist.txt, idp_ver.txt)

  recipe 목록  -> R3: sknn-planstep-r3 / M fab: ebeam_tas_lot_hist
  parameters   -> cdsem_idp_ver, full_name == recipe_id 의 최신 version

컬럼별 파생 규칙 (user-confirmed 2026-07-31)

  recipe_id     -> 스텝의 recipe_id 그대로. 같은 recipe 가 여러 스텝에 걸리면
                   행은 한 번만 냅니다(계약 단위가 recipe 이므로).
  ctn_desc      -> 그 recipe 가 걸린 **공정 스텝 이름**(R3 oper_desc /
                   M fab oper_det_desc). 문장을 지어내지 않습니다.
  prod_catg_cd  -> r3_device_grp 의 값. M fab device 는 이 컬럼이 없어
                   ""(빈 문자열)이며, 따라서 memory_class_auto 는 unknown 입니다.
  recipe_class  -> **recipe 이름이 "_S" 또는 "SE" 로 끝나면 Sample**, 아니면 Main.
                   끝자리 고정(anchored) 규칙입니다 — 이름 어디든 "SE" 를 찾으면
                   PHASE/BASE/SET 같은 평범한 단어가 전부 Sample 로 잡히고,
                   recipe_class 가 곧 프론트 cap 선택 축(D2)이라 위반 판정까지
                   뒤집힙니다. 스텝 코드(괄호 앞)로 판단하지 않습니다 —
                   오검출 가능성이 있다는 담당자 확인이 있었습니다.
  family        -> device 의 ctn_desc 에 "pool"/"풀" 이 있으면 Pool, 나머지는
                   전부 Core. **VG_RTC_Cubic 은 발행하지 않습니다** — 원천에서
                   판별할 근거가 없어, 틀린 분류보다 미분류를 택했습니다.
                   근거가 생기면 office 어댑터의 _family_of 만 고칩니다.
                   OFFICE-VERIFY: 요구사항 원문(ground_rules.txt 31행)은 VG 토큰이
                   ctn_desc 에 있다고 하여 위 2026-07-31 확인과 부딪힙니다.
                   대안 신호 "phase 토큰 없음 -> VG" 는 검토했으나 미채택입니다 —
                   사유는 office_example.py 의 _family_of docstring.
                   Pool 과 phase 는 한 ctn_desc 에 같이 옵니다("DRAM Pool제
                   (@Spica PV)"); 판정과 화면 칩 모두 Pool 이 이깁니다.
  phase         -> device 의 ctn_desc 에서 파싱합니다. "t-EV"/"tev" 를 "EV"
                   보다 먼저 봐야 합니다(부분 일치 함정).
                   OFFICE-VERIFY: 원천에 실제로 있는 "p-EV" 는 계약 Literal
                   (t-EV/EV/TV/PV)에 없어 현재 null 이 됩니다. 화면에 필요하면
                   계약을 넓혀야 합니다.

파라미터 이름의 세분도 — OFFICE-VERIFY

idp_ver.txt 와 같은 경고입니다. parameters blob 의 key 가 "WAFER"/"EDGE" 처럼
타입 단어뿐이면 위 26행의 longest-prefix 타입 파생이 할 일이 없어지고, 위 Mock
생성 규칙 2·3번이 전제하는 **bloated recipe·outlier 신호가 입력을 잃습니다.**
첫 사무실 실행에서 실제 key 목록을 확인하십시오.
