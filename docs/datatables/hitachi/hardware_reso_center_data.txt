장비의 상태 모니터링. bm/pm시에 체크하고 data로 남기게 되어있다.
beam_shape, reso_center에 해당. reso_center는 CD-SEM 장비에서만 제공된다.

OpenSearch index alias: reso_center_cdsem (category "reso_center_log").
(office 확인 2026-07-27 — 사무실 어댑터가 이 alias 로 실제 조회에 성공)

★ alias 와 category 를 혼동하지 말 것.
  조회하는 index(alias)  = reso_center_cdsem
  문서 안의 category 값  = reso_center_log
  "reso_center_log" 는 index 이름이 **아니다**. 이것을 index 로 조회하면 존재하지
  않는 index 이므로 조회가 실패한다.

  이것은 실제로 있었던 오류다. 2026-07-27 이전까지 back_dev_home 의 reso_center
  템플릿과 hardware/MIGRATION.md, 그리고 설계 문서까지 모두 reso_center_log 를
  index 이름처럼 적고 있었다. 코드 전체가 한 가지로 일치했지만 그 일치가 곧
  정확함은 아니었고, 옳게 적혀 있던 것은 이 문서 하나뿐이었다. 코드와 이 문서가
  어긋나면 이 문서가 기준이다.

doc 하나에는 다음과 같은 필드가 있다.
'category': str "reso_center_log"
'CenterX': float 예 = 1.15
'CenterY': float -0.99
'BestReso': float 2.98         # best-focus resolution (focus sweep의 최소값)
'ResoIScenter': float 3.04     # 중심 focus에서의 resolution (>= BestReso)
'ResoDelta': float 0.06        # ResoIScenter - BestReso 의 차이 (>= 0)
'beam_condition': str 'HR0500_IP0080'
'timestamp': str '2026-04-20T12:55:16'
'timestamp_date': str '2026-04-20'
'eqp_id'
'eqp_ip'
'fac_id'
'fab_name'

표시 방식:
- fab_name -> 해당 fab의 eqp_ip(장비)를 고르고, beam_condition별로 시계열을
  표시한다.
- 시계열 차트는 BestReso와 ResoIScenter 두 선을 같은 y축(nm)에 그리며, 두 선의
  간격이 곧 ResoDelta 이다.
- Focus Sweep은 Resolution Center에서 사용하지 않는다. 따라서 이전에 있던
  Resolution_Range / Resolution_Range_Raw / Resolution_Range_Smooth 와
  fdc_category 필드는 더 이상 사용하지 않는다.

========================================================================
어댑터 소비 규약 (hardware/reso_center office adapter)
========================================================================

★ 연결됨 — 사무실에서 구현 완료 (2026-07-27, 담당자 확인).
  구현체는 providers/reso_center/office.py 이며 gitignore 대상이라 집(home)
  workspace 에는 없습니다.
  ※ 2026-07-27: 집의 템플릿(office_example.py)은 이제 stub 이 아닙니다. 이 문서와
     형제 어댑터를 근거로 본문을 **재구성**해 채웠습니다. 사무실 사본과 같다는
     보장은 없으므로, 사무실에서 `cp` 하기 전에 기존 office.py 와 반드시 diff
     하십시오. 자세한 것은 hardware/MIGRATION.md.

build_reso_center_docs(eqp_id, fab_name, start, end) -> list[dict]

  - alias reso_center_cdsem 에서 [start, end] 구간을 timestamp 오름차순으로 raw doc
    그대로 돌려줍니다. 상위 dispatcher 가 normalizers.docs_payload 로 감쌉니다.
  - 이 index 는 eqp_ip 로 키가 걸려 있으므로, 다른 hardware 어댑터와 같은 방식으로
    eqp_id -> eqp_ip 를 해결합니다. 명부는 sem_list 입니다
    (docs/datatables/hitachi/sem_list.txt). sharpness 어댑터가 같은 문제를 이미 풀어
    두었으므로 같은 방식입니다.
    ★ 따라서 sem_list 도 office provider 여야 합니다. mock 명부는 가짜 IP 를 주고,
      그 IP 는 0건을 매칭해 "데이터 없음" 과 구분되지 않습니다.
  - fab_name 이 주어지면 필터에 겁니다.

돌려주는 field 는 위에 적힌 13개뿐입니다.

  ResoDelta 는 저장된 값을 그대로 통과시킵니다. 다시 계산하지 않습니다.
  Focus Sweep 은 제거되었으므로 Resolution_Range / Resolution_Range_Raw /
  Resolution_Range_Smooth 와 fdc_category 는 돌려주지 않습니다. mapping 이
  enabled:false 라 _source 에는 여전히 따라오므로, doc 에서 **떼어내야** office
  payload 가 mock 과 같은 모양이 됩니다.

CD-SEM 전용입니다.

템플릿 재구성에서 확인이 필요한 지점 (OFFICE-VERIFY)

  아래 두 가지는 집에서 확인할 수 없어 형제 index 의 관례를 따라 가정했습니다.
  사무실 office.py 와 다르면 사무실 쪽이 맞습니다.

  1. eqp_ip / fab_name 을 .keyword subfield 로 매칭한다고 가정했습니다.
     beam_shape 가 그렇게 하기 때문인데, 형제 index 끼리 규칙이 갈립니다 —
     sharpness_monitor_cdsem 의 ip 는 bare keyword 로 명시 매핑되어 있습니다.
     이 index 도 bare keyword 라면 .keyword 를 떼야 term 이 매칭됩니다.
  2. timestamp 가 offset 없는 KST wall clock 이라고 가정했습니다. Z 접미사가
     붙어 저장되면 조회 창이 9시간 밀립니다.
