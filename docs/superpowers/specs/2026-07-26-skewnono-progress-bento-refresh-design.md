# SKEWNONO 진행 보고 Bento 갱신 설계

Date: 2026-07-26
Artifact: `docs/progress_report/SKEWNONO_Progress.bento.html`
Reporting cutoff: `f03c2d9` (`2026-07-26 14:30:08 +0900`, 995 commits)

## 목표

기존 15장 Bento 발표 자료의 이야기 흐름과 시각 체계를 유지하면서, 2026년
7월 24일 이후 완료된 작업과 현재 저장소 지표를 반영합니다. 보고 기준은
사용자가 승인한 `f03c2d9` 커밋으로 고정하며, 그 이후 커밋과 작업 중 변경은
포함하지 않습니다.

## 편집 원칙

- `bento_agents.md`에 따라 `#bento-doc` JSON만 수정하고 나머지 HTML 런타임은
  그대로 둡니다.
- 기존 15장 구성, 슬라이드 ID, `morph` 전환, 흰색 테마, 파란색 강조색,
  글꼴 체계를 유지합니다.
- 숫자는 `f03c2d9`의 깨끗한 스냅샷에서 다시 계산한 값만 사용합니다.
- office 상태는 `어댑터 구현`과 `사내 실데이터 검증`을 구분합니다.
- 완료되지 않은 첫 클라우드 배포와 사내 28개 대표 계약 실행은 완료로
  표현하지 않습니다.
- 모든 슬라이드의 발표자 노트를 본문 변경과 일치시킵니다.

## 기준 지표

| 지표 | 값 | 근거 |
| --- | --- | --- |
| 전체 커밋 | 995 | `git rev-list --count f03c2d9` |
| 2026년 7월 커밋 | 673 | `git log` 월별 집계 |
| Nuxt 화면 | 54 | `front-dev-home/app/pages/**/*.vue` |
| 재사용 컴포넌트 | 186 | `front-dev-home/app/components/**/*.vue` |
| Flask API 규칙 | 74 | 깨끗한 스냅샷의 `app.url_map` `/api/*` 규칙 |
| 백엔드 테스트 | 1,328 | 깨끗한 스냅샷의 pytest 수집 결과 |
| 프론트엔드 테스트 | 778 | 깨끗한 스냅샷의 `npm test` 결과 |
| 전체 자동 테스트 | 2,106 | 백엔드 1,328 + 프론트엔드 778 |
| office 어댑터 템플릿 | 30 | `office_example.py` 전체 검색 |
| 구현된 office 템플릿 | 18 | `sync_office_adapters.py`의 stub 판별 |
| 미구현 stub 템플릿 | 12 | 위 30개 중 미구현 템플릿 |
| 사내 실데이터 검증 기능 | 2 | `docs/office-migration/STATUS.md`의 `office` 상태 |

## 슬라이드별 변경

### 표지와 요약

- `s1`: 커밋을 995로, 백엔드 기능을 런타임 registry 기준 22로 수정합니다.
  기존의 `사내 데이터 연결 완료 6`은 `실데이터 검증 완료 2`로 바로잡습니다.
- `s2`: 화면 54, 컴포넌트 186, API 74, 자동 테스트 2,106으로 수정합니다.
- `s5`: 규모를 설명하는 문장의 화면·컴포넌트 수를 현재 값으로 맞춥니다.

### 환경 전환과 배포

- `s7`: Phase 3을 단순 예정이 아니라 `패키징 준비 완료 · 첫 배포 대기`로
  표현합니다. 번들 allowlist, 사전 점검, manifest, 배포 runbook 구현이
  완료됐지만 실제 클라우드 배포는 남아 있음을 명시합니다.
- `s8`: 30개 템플릿, 자동 setup/sync, `STALE`/`EDITED` 감지를 반영합니다.
  office 파일 복사 방식과 응답 계약 고정이라는 기존 설명은 유지합니다.

### 개발 속도와 화면

- `s10`: 7월 막대 값을 486에서 673으로 수정합니다. 기존 bar chart와
  모션은 유지합니다.
- `s11`: 공통 화면 설명에 새 Mag/Pixel 가이드를 추가하고 화면 수를
  54개로 맞춥니다.

### office 전환 현황

- `s12` 제목을 `사내 연결 현황 — 구현과 실검증 분리`로 바꿉니다.
- 기존 20개 기능의 `6/13/1` 막대는 30개 템플릿의 `18 구현/12 stub` 막대로
  교체합니다.
- `sem_list`, `storage` 두 기능만 ledger에서 실데이터 검증 완료라는 점을
  별도 강조합니다.
- 하단 표에는 최근 완료된 office·배포 기반을 요약합니다.
  - MSR 이미지 비동기 작업·FTP/MinIO 전달
  - Hardware의 BM/PM·BSM·SCE·Sharpness 어댑터
  - 28개 대표 계약의 로컬 baseline과 사내 실행 대기
  - 클라우드 패키징·사전 점검·runbook 완료

### 7월 성과와 다음 단계

- `s13`: 7월 항목을 office 연결만으로 설명하지 않고 다음 완료 작업으로
  갱신합니다.
  - Skewvoir FDC sparkline matrix, 키보드 탐색, 다중 선택과 색상 식별
  - CD-SEM Mag/Pixel 계산·시각화 가이드
  - backend/frontend 테스트 확대와 CI gate
  - 배포 패키징·사전 점검
- `s14`: 다음 목표인 MCP와 매뉴얼 RAG는 유지하되, 현재 provider와 chat
  골격 위에서 이어지는 단계임을 설명합니다.
- `s15`: 54개 화면, 22개 백엔드 기능, 2,106개 테스트, 18개 구현 템플릿,
  2개 실데이터 검증, 배포 패키징 준비 완료를 최종 요약에 반영합니다.

## Bento 표현과 검증

- 기존 공유 element ID와 `transition: "morph"`를 보존합니다.
- `s10`은 숫자 비교이므로 기존 chart element를 유지합니다.
- `s1`, `s2`의 headline 숫자는 기존 `countUp` 효과를 유지합니다.
- office 현황은 서로 다른 분모를 한 막대에 섞지 않습니다. 30개 템플릿의
  구현 상태는 막대로, 22개 registry 기능 중 실데이터 검증 2개는 별도
  문장과 표로 표현합니다.
- JSON 안의 `<`는 `\u003c`로 이스케이프하며, HTML의 `#bento-doc` 외 영역이
  바뀌지 않았는지 비교합니다.
- 최종 검증은 JSON 파싱, Bento 스키마 핵심 필드, 슬라이드·ID·notes,
  margin, chart numeric data, `git diff --check`, Markdown lint로 수행합니다.

## 범위 밖

- 슬라이드 추가 또는 삭제
- 테마·폰트·색상·전환 방식 재설계
- Bento 런타임과 압축 자산 수정
- `f03c2d9` 이후 커밋이나 작업 중 변경 반영
- 실제 사내 28개 대표 계약 실행 또는 첫 클라우드 배포를 완료로 표기
