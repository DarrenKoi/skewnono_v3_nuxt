# raw/incidents/

장애·이슈 회고 (post-mortem).

- 파일명: `YYYYMMDD-<incident-title>.md`
- 형식: 타임라인 → 영향 → 근본원인 → 단기조치 → 장기조치
- 비난 없는(blameless) 톤.
- 고객 PII·내부 장비 식별자 제거.

ingest 시 LLM 이 `wiki/runbooks/` (재발 방지 절차) 또는 `wiki/decisions/` (구조적 변경) 에 반영한다.
